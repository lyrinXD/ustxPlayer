# 更新日志

> [!TIP]
> 本文档部分使用AI编写

## v26g30 (2026-07-30) — ustxPlayer 二次开发版

基于 [ustPlayer](https://github.com/SYEternalR/ustPlayer) v26f19 的二次开发版本。核心工程格式从 UST 全面转向 USTX，并新增样式系统、多语言 LRC 歌词、音频同步播放、界面主题定制等功能。

---

### 🎼 工程格式支持

- **USTX 原生支持**：抛弃脆弱的 UST 格式，直接支持 OpenUtau `.ustx` 工程文件（基于 YAML 解析）。解析过程自动处理编码，用户无需手动尝试编码。
- **多语言文本支持**：工程文件中的歌词与项目信息理论上支持所有语言文本的解析与显示。
- **UST 兼容方案**：旧版 `.ust` 用户可通过外部工具（如 [UtaFormatix](https://utaformatix.tk/)）转换后再导入使用。
- **多线程解析**：工程解析移至后台线程，大文件导入时界面不再卡顿。

---

### 📦 工程文件系统重构（不兼容旧版）

- **格式版本升级**：`.uplr` 工程文件采用全新格式（`format: "ustxPlayer.uplr"`，`version: 2`），**不兼容旧版 ustPlayer 的 `.uplr` 文件**，导入旧版文件会被拒绝并提示格式不匹配。
- **内嵌 USTX 内容**：导出时将 `.ustx` 工程文件全文以 `ustx_content` 字段内嵌于 `.uplr` 中，使其成为独立完整的工程文件，分发时无需额外携带 `.ustx` 文件。
- **缓存释放机制**：导入 `.uplr` 时将内嵌的 USTX 内容释放到程序目录下的 `cache/` 子目录并重新解析得到音符数据；程序启动与退出时各清空一次 `cache/`，确保干净状态并避免残留。

---

### 🎨 样式系统

- **逐字样式定义**：每个歌词字都可以独立配置样式属性，实现精细化的视觉控制。
- **样式编辑界面**：新增直观快捷的样式编辑页，支持单字编辑与批量编辑。
- **批量编辑功能**：可一次性选中多个字或全部字，统一应用样式修改。
- **全局背景覆盖**：支持启用全局背景色，并可选择覆盖所有逐字样式，方便后期剪辑（但实测好像不是很方便）。

---

### ▶️ 播放体验

- **音频同步播放**：支持导入音频文件，在可视化播放时与工程同步输出，更直观的同时方便在剪辑软件中精确对轨。
- **LRC 字幕同步**：支持导入 `.lrc` 歌词文件，与音频和画面同步显示。
- **播放控制**：
  - 空格键：播放 / 暂停
  - `←` / `→`：快退 / 快进 10 秒
  - `↑` / `↓`：系统音量调节
  - `X` / `C`：倍速减 0.1 / 加 0.1
  - `Z`：还原 1 倍速
  - `ESC`：退出播放器
- **暂停与进度控制**：播放器支持暂停、快进、快退，方便反复查看某一片段。
- **音量与倍速调节**：播放过程中可直接调节系统音量和播放倍速。

---

### 🌍 多语言 LRC 歌词

- **交错格式支持**：识别同一时间点连续排列的多语言行，自动分组显示。
- **独立格式支持**：识别按语言分块排列的 LRC 文件，排序后自动对齐同一时间点。
- **合并格式兼容**：合并格式不常见，故不做专门支持，但大多数情况下可正常显示。
- **无限行数支持**：渲染逻辑采用通用循环，理论上支持任意行数，推荐 1~3 行以获得最佳显示效果。
- **自适应布局**：多行歌词会根据屏幕分辨率自动计算字号与行间距，并自动远离中央逐字大字以避免重叠。

---

### ⚙️ 界面与自定义

- **强调色设置**：用户可自定义界面强调色，支持跟随系统或手动指定颜色。
- **深色模式适配**：软件适配深色环境。现界面主题支持跟随系统、亮色、暗色三种模式。
- **字体自定义**：
  - 逐字歌词字体（中央大字）
  - 歌词及信息字体（LRC、音符信息、版权等）
  - 支持加载自定义 `.ttf` / `.otf` 字体文件
- **信息颜色**：可自由修改信息文字颜色，LRC 歌词复用该颜色以保持统一。
- **版权信息开关**：用户可通过选项隐藏播放器底部的版权信息。
- **静默显示选项**：静默显示新增 `♪` 符号选项。
- **标签页调整**：重新组织了侧边导航与设置页的结构，分类更加合理。
- **外部工具链接**：在“其他”页新增多个相关工具入口：

  - [163MusicLyrics](https://github.com/jitwxs/163MusicLyrics/) — 网易云歌词下载
  - [Vocal2Midi](https://www.bilibili.com/video/BV1Ww7C6kEqL/) — 人声转 MIDI
  - [signal](https://signalmidi.app/edit) — MIDI 在线编辑

---

### 🐛 修复与优化

- **修复拖拽文件不生效的问题**：主窗口和文件页均支持拖拽导入 `.ustx`、`.uplr`、`.lrc` 等文件。
- **汉化残留英文**：修复了字体选择对话框、导航提示等位置的英文显示问题，统一为中文界面。
- **界面响应优化**：通过多线程解析和更合理的信号驱动，减少大工程导入时的界面卡顿。

---

### 🏗️ 项目结构

```
ustxPlayer/
├── main.py                     # 程序入口
├── requirements.txt            # 依赖声明
├── pyrightconfig.json          # Pyright 类型检查配置（pythonVersion / extraPaths / typeCheckingMode）
├── core/
│   ├── settings_manager.py     # 配置与 .uplr 项目管理
│   ├── ustxreader.py           # USTX 工程解析器
│   ├── ustxplayer.py           # 全屏播放器（QPainter 渲染）
│   ├── i18n.py                 # 界面汉化翻译器
│   └── log.py                  # 日志系统（loguru）
├── ui/
│   ├── basic_page.py           # 基础 — 项目信息、显示选项、播放
│   ├── file_page.py            # 文件 — USTX 选择、解析、预览
│   ├── player_style_page.py    # 播放器 — 颜色、字体、显示设置
│   ├── lyric_edit_page.py      # 歌词 — 逐字样式编辑、批量编辑
│   └── other_page.py           # 其他 — 主题、快捷键、外部工具、关于软件
```

> 相比 ustPlayer v26f19，核心解析器从 `ustreader.py` 迁移至 `ustxreader.py`，播放器从 `ustplayer.py` 重构为 `ustxplayer.py`，并新增 `lyric_edit_page.py` 与 `i18n.py` 以支持样式系统与界面汉化。

---

### 📦 依赖

```
PySide6 >= 6.5.0
PySide6-Fluent-Widgets[full] >= 1.11.2
loguru >= 0.7.0
PyYAML >= 6.0
```

> 详见 [`requirements.txt`](requirements.txt)。
>
> 相比 v26f19，numpy 依赖已被移除。旧版 UST 播放器用 numpy 做音符位置的序贯累加和数组运算；v26g30 全面转向 USTX 格式后，音符自带 `position` 绝对定位字段，音高线渲染改用纯 Python 遍历，numpy 不再需要。

---

>[!CAUTION]
>此版本为框架级重写，可能包含未发现的稳定性问题，请谨慎使用。

## v26f19 (2026-06-19) — 全新 PySide6 现代化重制版

ustPlayer 从 tkinter 全面迁移至 PySide6 + Fluent Design，带来现代化的界面体验和更好的性能表现。

---

### 🎨 界面焕新

- **侧边导航栏**：用图标 + 文字的侧边导航替代了原来的手绘标签页，切换更流畅、视觉更清晰。
- **Fluent Design 风格**：采用微软 Fluent Design System 组件库，按钮、输入框、下拉框、开关等控件外观全面升级。
- **Switch 开关**：原来基础页的复选框全部换成 Fluent 风格的滑动开关，更直观、更好看。
- **InfoBar 非阻塞通知**：操作成功/失败的提示改为顶部滑入的 InfoBar，不再弹窗打断你的操作。
- **内置取色器**：颜色选择从系统对话框升级为 Fluent 风格的内置取色面板，点击即出，同时支持手动输入 hex 色值，双向同步。

---

### ✨ 新增功能

- **音高线独立颜色**：新增「音高线颜色」选项，不再与"其他文字色"共用，6 种颜色全部独立可配。
- **日志系统**：新增 loguru 日志模块，文件（DEBUG 级别）+ 控制台（INFO 级别）双输出，播放异常时可以查看日志定位问题。
- **编码检查按钮**：切换编码后不再自动刷新预览，改为点击「编码检查」按钮手动触发——切换编码和检查预览互不干扰。
- **自定义文字**：音高间占位符、静默显示、结束显示均支持「自定义文字」选项，输入你想显示的任何内容。
- **项目文件完整支持**：`.uplr` 工程文件的导入/导出覆盖全部 6 个配置段（编码、基础、显示、颜色、歌词、额外设置），不再遗漏任何配置。

---

### 🔧 优化改进

- **文本渲染**：CJK 字形不再被裁切——使用 `QFontMetrics` 精确测量并预留 20% 边距。
- **全屏播放器**：
  - QPainter 硬件加速渲染，原生抗锯齿，画面更细腻。
  - 窗口边缘不再漏出桌面（窗口标志在显示前统一设置）。
  - 窗口大小变化时实时更新渲染坐标，不会偏移。
  - 5ms 高精度 QTimer 刷新，播放节奏更准确。
- **UST 解析器**：代码更健壮，文件自动关闭、异常信息更清晰。
- **非模态确认弹窗**：启动播放器前的确认对话框采用 MessageBox，点取消不会再意外启动播放器。
- **检查编码**：检查编码时需要点击按钮，防止自动检查时卡死 - #1 
- **协议文件改名**：协议文件拆分成两个独立文件，其中一个改名为LICENSE以在GitHub中直接展示 - #3 

---

### 🐛 已修复

- **取消播放**：旧版在确认弹窗点取消后仍有可能启动播放器，现已修复。
- **播放结束自动关闭**：修复了播放结束后定时器竞态可能导致窗口残留的问题。
- **音高线颜色**：修复了音高曲线颜色与"其他文字色"共用的历史问题，现为独立配置项。

---

### 🏗️ 项目结构

```
ustPlayer/
├── main.py                     # 程序入口
├── requirements.txt            # 依赖声明
├── core/
│   ├── settings_manager.py     # 配置管理器（信号驱动）
│   ├── ustreader.py            # UST 文件解析器
│   ├── ustplayer.py            # 全屏播放器（QPainter 渲染）
│   └── log.py                  # 日志系统（loguru）
├── ui/
│   ├── basic_page.py           # 基础 — 项目信息、显示选项、播放
│   ├── file_page.py            # 文件 — UST选择、编码检查、预览
│   ├── player_style_page.py    # 播放器 — 6色选择、显示设置
│   ├── lyric_page.py           # 歌词 — LRC导入、显示开关
│   └── other_page.py           # 其他 — 版权、工具、协议
└── ...
```

> 对比旧版单文件 1149 行的 `main.py`，新版拆分为模块化结构，便于维护和扩展。

---

### 📦 依赖

```
PySide6 >= 6.5.0
PySide6-Fluent-Widgets[full] >= 1.5.0
loguru >= 0.7.0
numpy >= 1.26.0
```

> 相比旧版仅依赖 Python 标准库 + tkinter，新版需要安装以上第三方包。详见 `requirements.txt`。
